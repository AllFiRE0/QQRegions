<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.qqregions</groupId>
    <artifactId>QQRegions</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>QQRegions</name>
    <description>Мощное управление регионами поверх WorldGuard (Leaf 26.2+)</description>
    <url>https://github.com/AllF1RE</url>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>

        <!-- API ядра Leaf 26.2 (форк Paper). https://github.com/Winds-Studio/Leaf -->
        <leaf.version>26.2.local-SNAPSHOT</leaf.version>

        <!-- WorldGuard для MC 26.2 (7.0.18). Если появится новее — обнови. -->
        <worldguard.version>7.0.18</worldguard.version>

        <placeholderapi.version>2.11.6</placeholderapi.version>
    </properties>

    <repositories>
        <repository>
            <id>leafmc</id>
            <url>https://maven.leafmc.one/snapshots/</url>
        </repository>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>enginehub</id>
            <url>https://maven.enginehub.org/repo/</url>
        </repository>
        <repository>
            <id>extendedclip</id>
            <url>https://repo.extendedclip.com/releases/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Leaf / Paper API -->
        <dependency>
            <groupId>cn.dreeam.leaf</groupId>
            <artifactId>leaf-api</artifactId>
            <version>${leaf.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- WorldGuard API (языковая база регионов) -->
        <dependency>
            <groupId>com.sk89q.worldguard</groupId>
            <artifactId>worldguard-bukkit</artifactId>
            <version>${worldguard.version}</version>
            <scope>provided</scope>
            <exclusions>
                <!-- bstats-bukkit:1.7 нет в репозиториях, а при компиляции не нужен -->
                <exclusion>
                    <groupId>org.bstats</groupId>
                    <artifactId>bstats-bukkit</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- PlaceholderAPI (мягкая зависимость) -->
        <dependency>
            <groupId>me.clip</groupId>
            <artifactId>placeholderapi</artifactId>
            <version>${placeholderapi.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>QQRegions-${project.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>25</release>
                    <encoding>UTF-8</encoding>
                    <!-- Обход бага javac-in-process на JDK 25
                         ("Fatal error compiling: Cannot load from object array
                          because this.hashes is null") — CMOPL-404 -->
                    <fork>true</fork>
                    <forceLegacyJavacApi>true</forceLegacyJavacApi>
                </configuration>
            </plugin>
            <!-- ресурсы: только то, что реально лежит в src/main/resources -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>